# ============================================================
# Loops - Problem: Move Zeros to End
# Topics: list sorting, conditional reordering
# ============================================================

listing = [1, 2, 3, 4, 0, 0, 0, 12]
listing.sort(reverse=True)
print("Zeros moved to end:", listing)
# Output: [12, 4, 3, 2, 1, 0, 0, 0]
